apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: proxmox-backup
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/utilities/proxmox-backup/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

